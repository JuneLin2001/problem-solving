---
title: First non-repeating character
description: Write a function named first_non_repeating_letter† that takes a string input, and returns the first character that is not repeated anywhere in the string.
keywords: [Codewars, First non-repeating character, 5 kyu, Strings, Algorithms]
tags: [Codewars, 5 kyu, Strings, Algorithms]
---

# [{frontMatter.title}](https://www.codewars.com/kata/52bc74d4ac05d0945d00054e/train/javascript)

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import CodeBlock from "@theme/CodeBlock";
import DifficultyBadge from "@site/src/components/Badges/DifficultyBadge";
import Solution from "!!raw-loader!./solution.js";
import Description from "./_Description.md";

<DifficultyBadge kyu={5} />

<Tabs>
  <TabItem value="description" label="題目描述" default>
    <details open>
      <summary>Description</summary>
        <Description />
    </details> 
  </TabItem>

  <TabItem value="solution" label="解答">
   ## Solution
    <CodeBlock language="js">{Solution}</CodeBlock>
  </TabItem>
</Tabs>

### 解題思路

使用 Counter 的作法簡化邏輯，也實作了題目提到的額外挑戰:

> As an added challenge, upper- and lowercase letters are considered the same character,but the function should return the correct case for the initial letter.  
> For example, the input 'sTreSS' should return 'T'.

使用 Counter 統計的會是 `lowerStr`，但回傳的要是原本的 `s` 之中最早出現的那個 `s[i]`

### 心得

練習 Counter 的題目，也配養看到題目想出初步解法的能力。
